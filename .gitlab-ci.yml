build-job:
  tags:
    - Macos
  stage: build
  script:
    - echo "Hello, world"
    - pwd
    - terraform init

build-job1:
  tags:
    - Macos
  stage: build platform
  script:
    - terraform apply --auto-approve
    - terraform destroy --auto-approve
  when: manual
  