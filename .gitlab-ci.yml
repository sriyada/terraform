build-job:
  tags:
    - Macos
  stage: build
  script:
    - echo "Hello, world"
    - pwd
    - terraform init
    - terraform plan
    - terraform apply --auto-approve
    - terraform destroy --auto-approve